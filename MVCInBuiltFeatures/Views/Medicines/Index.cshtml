@model IEnumerable<MVCInBuiltFeatures.Models.Medicine>


@using MVCInBuiltFeatures.Controllers

@{
    ViewBag.Title = "ระบบคลังยา";
}

<h2>Index</h2>
@using (Html.BeginForm("Index", "Medicines", FormMethod.Get))
{
    <p>
        Title: @Html.TextBox("SearchString")
        <input type="submit" value="ค้นหาโดยชื่อยา" />
    </p>
}
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MedName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Size)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Unit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EXP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MedName)
        </td><input id="Button1" type="button" value="button" />
        <td>
            @Html.DisplayFor(modelItem => item.Rem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Size)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Unit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EXP)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID }) |
            @Html.ActionLink("Add", "Index", "Medicine", null, new { id = item.ID })
        </td>
    </tr>
    

    

}

</table>
